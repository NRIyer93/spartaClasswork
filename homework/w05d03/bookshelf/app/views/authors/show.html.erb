<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @author.name %>
</p>

<ul>
	<%= @author.books.each do |book| %>
		<li><%= book.name %> <%= link_to 'Show', show_book_path(@book) %></li>
	<% end %>
</ul>

<%= link_to 'Edit', edit_author_path(@author) %> |
<%= link_to 'Back', authors_path %>
